# Server-only. Never prefix these with NEXT_PUBLIC_ — the browser must not see them.
# Neon: the pooled connection string (host contains -pooler).
DATABASE_URL=
# Signs PIN access cookies. Generate with: openssl rand -hex 32
ACCESS_COOKIE_SECRET=
